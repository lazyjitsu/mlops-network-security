### Network Security Projects for Phishing Data

```
touch requirements.txt
mkdir -p .github/workflows/
touch .github/workflows/main.yaml

```


Note the **-e .** in the requirements.txt and the setup.py. Whe we run
```pip install -r requirements.txt``` i will see the **-e .** and run setup.py which will package our application!!

NetworkSecurity.egg-info package was created w/all the relevant info from setup.py (see pkg.info)
